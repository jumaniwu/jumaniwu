# ─────────────────────────────────────────────────────────────────────────────
# AI Affiliate Automation Agent — Python Dependencies
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── LLM Clients ───────────────────────────────────────────────────────────────
google-generativeai>=0.7.0          # Gemini 1.5 Flash (Google AI Studio)
groq>=0.9.0                         # Groq API (Llama 3 - fast fallback)

# ── HTTP & Async ──────────────────────────────────────────────────────────────
httpx>=0.27.0                       # Async HTTP client
aiofiles>=23.2.1                    # Async file I/O
fastapi>=0.111.0                    # Agent API server (for n8n HTTP triggers)
uvicorn[standard]>=0.30.0           # ASGI server for FastAPI

# ── Scraping ──────────────────────────────────────────────────────────────────
apify-client>=1.7.0                 # Apify SDK (Shopee scraper actor)
beautifulsoup4>=4.12.3              # Fallback HTML parser
lxml>=5.2.0                         # Fast XML/HTML parser for BS4

# ── Image Processing ─────────────────────────────────────────────────────────
Pillow>=10.3.0                      # Image manipulation
piexif>=1.1.3                       # EXIF metadata read/write/strip

# ── Telegram ──────────────────────────────────────────────────────────────────
python-telegram-bot[webhooks]>=21.0 # Telegram Bot API

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9              # PostgreSQL adapter

# ── Cache / Queue ─────────────────────────────────────────────────────────────
redis>=5.0.0                        # Redis client + pub/sub

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0                # Load .env files

# ── Reliability ───────────────────────────────────────────────────────────────
tenacity>=8.3.0                     # Retry with exponential backoff

# ── Logging ───────────────────────────────────────────────────────────────────
structlog>=24.1.0                   # Structured JSON logging

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dateutil>=2.9.0              # Date parsing
pytz>=2024.1                        # Timezone handling (Asia/Jakarta)
